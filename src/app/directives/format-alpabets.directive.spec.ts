import { FormatAlpabetsDirective } from './format-alpabets.directive';

describe('FormatAlpabetsDirective', () => {
  it('should create an instance', () => {
    const directive = new FormatAlpabetsDirective();
    expect(directive).toBeTruthy();
  });
});
